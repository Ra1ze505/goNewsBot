package config

// Channels - список каналов для мониторинга
// Note: These are the actual channel IDs (without the -100 prefix)
var Channels = []int64{
	1429590454, // @kontext_channel
	1754252633, // Топор Live
}

const SessionDir = "session/telegram-session"
